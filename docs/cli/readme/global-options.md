---
slug: "/cli/readme/global-options"
---

# Global Options

The CLI allows the use of the following optional global options:

* `-l, --log-level`: The log level to display. Choose from `debug`, `info`, `warn`, `error`, or `quiet`.
